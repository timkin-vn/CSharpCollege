<Window x:Class="FifteenGame.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:FifteenGame.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Крестики-нолики"
        Width="420" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF0B1020"
        Loaded="Window_Loaded">

    <Window.DataContext>
        <viewmodels:TicTacToeViewModel/>
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="16" Background="#141B2D" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Крестики-нолики" Foreground="#EAF0FF" FontSize="22" FontWeight="Bold"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="#A9B6DA" FontSize="14" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding UserName, StringFormat=Игрок: {0}}"
                               Foreground="#7281A8" FontSize="12" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border CornerRadius="12" Background="#0F1630" Padding="10,6" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ход: " Foreground="#A9B6DA" FontSize="14"/>
                            <TextBlock Text="{Binding CurrentPlayer}" Foreground="#EAF0FF" FontSize="14" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <Button Content="Новая игра"
                            Padding="12,8"
                            FontWeight="SemiBold"
                            Background="#5B7CFF"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="NewGameButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" CornerRadius="20" Background="#141B2D" Padding="18">
            <ItemsControl ItemsSource="{Binding Cells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid MouseDown="Cell_MouseDown" Tag="{Binding ColumnRow}">
                            <Border Margin="8" CornerRadius="16"
                                    Background="{Binding BruhButon}"
                                    BorderBrush="{Binding ColorButtonPen}"
                                    BorderThickness="2">
                                <TextBlock Text="{Binding TextButoon}"
                                           Foreground="{Binding TextColor}"
                                           FontSize="44"
                                           FontWeight="Black"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding Row}"/>
                        <Setter Property="Grid.Column" Value="{Binding Column}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="Кликни по клетке, чтобы поставить X или O."
                   Foreground="#7281A8"
                   FontSize="12"
                   Margin="4,14,4,0"
                   HorizontalAlignment="Center"/>
    </Grid>
    
</Window>
