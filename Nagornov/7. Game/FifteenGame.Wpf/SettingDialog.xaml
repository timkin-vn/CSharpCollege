<Window x:Class="FifteenGame.Wpf.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки игры" Height="200" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Настройки новой игры" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Количество мин:" FontSize="14" Margin="0,0,0,5"/>
        <Slider Grid.Row="2" x:Name="MinesSlider" Minimum="5" Maximum="30" Value="15" 
                TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Text="{Binding ElementName=MinesSlider, Path=Value, StringFormat='Выбрано мин: {0}'}" 
                   FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Отмена" Click="CancelButton_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="Начать игру" Click="StartButton_Click" Width="100" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>