<Window x:Class="CardFile.WPF.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardFile.WPF.Views" xmlns:viewmodels="clr-namespace:CardFile.WPF.ViewModels"
        mc:Ignorable="d"
        Title="AboutWindow" Height="400" Width="400" ResizeMode="NoResize">
    <Window.DataContext>
        <viewmodels:AboutViewModel/>
    </Window.DataContext>
    <DockPanel>
        
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right">
            <Button Content="OK" Margin="5" Padding="5" Click="OK_Click"></Button>
            <Button Content="Отмена" Margin="5" Padding="5" Click="Cancel_Click"></Button>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Copyright}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Version}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </StackPanel>
    </DockPanel>
</Window>
