<Window x:Class="CardFile.Wpf.Views.CardEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardFile.Wpf.Views"
        xmlns:viewmodels="clr-namespace:CardFile.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Редактирование заметки"
        Height="420" Width="520"
        WindowStartupLocation="CenterOwner">

    <Window.DataContext>
        <viewmodels:CardViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Заголовок:" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title}" Margin="5" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Категория:" VerticalAlignment="Center" Margin="5" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Category}" Margin="5" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Текст:" VerticalAlignment="Top" Margin="5" />
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Text}"
                 Margin="5"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Height="180" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Дата создания:" VerticalAlignment="Center" Margin="5" />
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding CreatedAt}" Margin="5" />

        <CheckBox Grid.Row="4" Grid.Column="1" IsChecked="{Binding IsDone}" Margin="5" Content="Выполнено" />
        <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding IsPinned}" Margin="5" Content="Закреплено" />

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right">
            <Button Content="OK" Padding="5" Margin="5" Click="OkButton_Click" />
            <Button Content="Отмена" Padding="5" Margin="5" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>