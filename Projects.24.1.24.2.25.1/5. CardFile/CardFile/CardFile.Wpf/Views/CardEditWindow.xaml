<Window x:Class="CardFile.Wpf.Views.CardEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Карточка фильма" Height="340" Width="380" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Padding="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Поля ввода характеристик фильма -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="0">
            <StackPanel>
                <Label Content="Название фильма:" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="23"/>

                <Label Content="Режиссер:" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Director, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="23"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <Label Content="Год выпуска:" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Year, UpdateSourceTrigger=PropertyChanged}" Height="23"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Label Content="Длительность (мин):" Margin="0,0,0,2"/>
                        <TextBox Text="{Binding Duration, UpdateSourceTrigger=PropertyChanged}" Height="23"/>
                    </StackPanel>
                </Grid>

                <Label Content="Жанр:" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="23"/>

                <Label Content="Рейтинг:" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Rating, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0.0}}" Height="23"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки ОК / Отмена -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="OkButton" Content="OK" Width="85" Height="25" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="85" Height="25" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
