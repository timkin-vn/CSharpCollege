<Window x:Class="CardFile.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardFile.Wpf" 
        xmlns:viewmodels="clr-namespace:CardFile.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="500" Width="900" 
        Loaded="Window_Loaded" 
        Initialized="Window_Initialized">

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Файл">
                <MenuItem Header="_Открыть..." Click="FileOpenMenuItem_Click" />
                <MenuItem Header="_Сохранить" Click="FileSaveMenuItem_Click" />
                <MenuItem Header="Со_хранить как..." Click="FileSaveAsMenuItem_Click" />
                <Separator />
                <MenuItem Header="В_ыход" Click="FileExitMenuItem_Click" />
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#F0F0F0" Padding="10" Margin="5" CornerRadius="3">
                <DockPanel>
                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             VerticalContentAlignment="Center" Height="25" />
                </DockPanel>
            </Border>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Companies}" 
                      SelectedItem="{Binding SelectedCompany}" 
                      SelectionChanged="DataGrid_SelectionChanged"
                      IsSynchronizedWithCurrentItem="True" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False" 
                      SelectionMode="Single" 
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Название организации" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="ИНН" Binding="{Binding TaxId}" Width="120" />
                    <DataGridTextColumn Header="Отрасль" Binding="{Binding Industry}" Width="150" />
                    <DataGridTextColumn Header="Директор" Binding="{Binding CEO}" Width="150" />
                    <DataGridTextColumn Header="Сотрудников" Binding="{Binding EmployeeCount}" Width="100" />
                    <DataGridTextColumn Header="Выручка" Binding="{Binding AnnualRevenue, StringFormat={}{0:N2} руб.}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button Content="Добавить компанию" Padding="10,5" Margin="5" Click="CreateButton_Click" />
                <Button Content="Изменить" IsEnabled="{Binding IsEditButtonEnabled}" 
                        Padding="10,5" Margin="5" Click="EditButton_Click" />
                <Button Content="Удалить" IsEnabled="{Binding IsDeleteButtonEnabled}" 
                        Padding="10,5" Margin="5" Click="DeleteButton_Click" />
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>