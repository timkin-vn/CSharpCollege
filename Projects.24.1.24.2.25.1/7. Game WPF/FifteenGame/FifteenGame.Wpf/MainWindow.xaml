<Window x:Class="FifteenGame.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FifteenGame.Wpf" 
        xmlns:viewmodels="clr-namespace:FifteenGame.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="2048" Height="550" Width="550" MinHeight="450" MinWidth="450"
        KeyDown="OnGridKeyPress"
        Background="#1A2E1A">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="Score: " FontSize="24" FontFamily="Segoe UI Semibold" Foreground="#E0F2E9" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Score}" FontSize="28" FontFamily="Segoe UI Black" Foreground="#4EFEAA" VerticalAlignment="Center" Margin="5,0,20,0"/>
            <Button Content="New Game" FontSize="16" FontFamily="Segoe UI Semibold" Padding="15,6" 
                    Background="#2D5A27" Foreground="#E0F2E9" BorderBrush="#4EFEAA" BorderThickness="1.5"
                    Cursor="Hand" Click="OnRestartAction">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{Binding GameStatus}" FontSize="18" FontFamily="Segoe UI Black" Foreground="#FF4B4B" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <Border Grid.Row="2" Background="#0F1F0F" CornerRadius="12" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl ItemsSource="{Binding GridItems}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="95"/>
                                <RowDefinition Height="95"/>
                                <RowDefinition Height="95"/>
                                <RowDefinition Height="95"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="95"/>
                                <ColumnDefinition Width="95"/>
                                <ColumnDefinition Width="95"/>
                                <ColumnDefinition Width="95"/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="4" Background="{Binding BackgroundBrush}" CornerRadius="10" BorderBrush="#2D5A27" BorderThickness="1">
                            <TextBlock Text="{Binding Text}" FontSize="34" FontFamily="Segoe UI Black" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="#1A2E1A"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding Row}"/>
                        <Setter Property="Grid.Column" Value="{Binding Column}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>
    </Grid>
</Window>