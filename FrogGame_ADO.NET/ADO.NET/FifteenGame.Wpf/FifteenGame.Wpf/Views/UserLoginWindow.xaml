<Window x:Class="FifteenGame.Wpf.Views.UserLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FifteenGame.Wpf.Converters"
        Title="Enter Game" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Frog Game" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Enter your name:" 
                   FontSize="14" Margin="0,0,0,5"/>

        <TextBox Grid.Row="2" x:Name="UserNameTextBox" 
                 FontSize="14" Padding="5" 
                 Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                 MaxLength="50">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}"
                   Foreground="Red" FontSize="12" 
                   Margin="0,10,0,0" TextWrapping="Wrap"
                   Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
			<Button Content="Login" Command="{Binding LoginCommand}"
		 Padding="15,5" FontSize="14"
		 IsDefault="True" Margin="0,0,10,0"/>

			<Button Content="Cancel" Command="{Binding CancelCommand}"
					Padding="15,5" FontSize="14"
					IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>