<Window x:Class="FifteenGame.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="2048" Height="520" Width="520">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Пользователь: " FontSize="14" />
                    <TextBlock Text="{Binding UserName}" FontSize="14" FontWeight="Bold" />
                </StackPanel>

                <Button Content="Выйти из аккаунта"
                        Width="160"
                        Padding="8,3"
                        Margin="0,6,0,8"
                        Click="Logout_Click"/>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Рекорд: " FontSize="16" FontWeight="Bold" />
                    <TextBlock Text="{Binding BestScore}" FontSize="16" FontWeight="Bold" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="Score: " FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="{Binding Score}" FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="   Moves: " Margin="12,0,0,0" FontSize="18" />
                    <TextBlock Text="{Binding MoveCount}" FontSize="18" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top">
                <Button Content="Новая игра" Padding="10,4" Margin="0,0,6,0" Click="NewGame_Click"/>
                <Button Content="Выход" Padding="6,2" FontSize="12" Height="26" MinWidth="60" Click="Exit_Click"/>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Background="#BBADA0"
                CornerRadius="12"
                Padding="10"
                MouseLeftButtonDown="Field_MouseDown"
                MouseLeftButtonUp="Field_MouseUp">

            <ItemsControl ItemsSource="{Binding Cells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6" CornerRadius="8" Background="LightBlue">
                            <TextBlock Text="{Binding Text}"
                                       FontSize="28"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding Row}" />
                        <Setter Property="Grid.Column" Value="{Binding Column}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Border>

    </Grid>
</Window>