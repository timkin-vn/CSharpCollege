<Window x:Class="_2048Game.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:_2048Game.WPF.ViewModels" xmlns:local="clr-namespace:_2048Game.WPF"
        mc:Ignorable="d"
        Title="2048" Height="668" Width="520"
        Background="#faf8ef"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <local:TileForegroundConverter x:Key="TileForegroundConverter"/>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="ButtonStyleRestart" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <viewmodels:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="20,20,28,102">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Кол-во нажатий: "
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="#776e65"
                       DockPanel.Dock="Left" Width="420" Height="50"/>
        </DockPanel>
        <StackPanel Orientation="Horizontal" Margin="337,0,27,20">
            <TextBlock Text="{Binding MoveCount}"
                       Width="95"
                       Height="50"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="#776e65"/>
        </StackPanel>
        <Button Style="{DynamicResource ButtonStyleRestart}" Content="Перезапуск игры" 
                HorizontalAlignment="Center" 
                Width="450" 
                Height="53"
                Background="#776e65"
                Foreground="#faf8ef"
                FontSize="25"
                FontWeight="Bold" Margin="0,445,0,-65" Grid.Row="1" Click="RestartGameBtn_Click" Focusable="False"/>
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="#bbada0" CornerRadius="10" Width="450" Height="400">
                <Grid>
                    <UniformGrid Rows="4" Columns="4" Margin="0">
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                        <Border Background="#cdc1b4" CornerRadius="6" Margin="5"/>
                    </UniformGrid>
                    <ItemsControl ItemsSource="{Binding Tiles}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="4" Columns="4"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="6" Margin="5" Background="{Binding Background}" Focusable="False">
                                    <TextBlock Text="{Binding Value}"
                                           FontSize="32"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Foreground}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Value}" Value="0">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>