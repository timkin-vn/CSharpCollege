<Window x:Class="_2048Game.WPF.Views.RegistrationOrEnterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_2048Game.WPF.Views" xmlns:viewmodels="clr-namespace:_2048Game.WPF.ViewModels"
        mc:Ignorable="d"
        Title="Вход или регистрация" Height="500" Width="500" Background="#faf8ef">
    <Window.DataContext>
        <viewmodels:RegistrationOrEnterWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#625B53"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="15" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="2048"
               FontSize="48"
               FontWeight="Bold"
               Foreground="#776e65"
               DockPanel.Dock="Left" Width="123" Height="422" RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleY="1.226"/>
                        <RotateTransform/>
                        <TranslateTransform Y="1.38"/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
            </TextBlock>
        </DockPanel>
        <DockPanel Margin="10,143,0,0">
            <TextBlock
                Text="Логин: " 
                FontSize="30"
                FontWeight="Bold"
                Foreground="#776e65"
                DockPanel.Dock="Left" Width="474">
            </TextBlock>
        </DockPanel>
        <DockPanel Margin="0,0,0,0">
            <TextBlock
                Text="Пароль: " 
                FontSize="30"
                FontWeight="Bold"
                Foreground="#776e65"
                DockPanel.Dock="Left" Width="469" Height="83">
            </TextBlock>
        </DockPanel>
        <DockPanel Margin="140,0,10,135">
            <TextBox Height="29" Width="400" x:Name="LoginBoxUserName" FontSize="20">
            </TextBox>
        </DockPanel>
        <DockPanel Margin="140,0,10,40">
            <PasswordBox Height="30" Width="390" FontSize="20" x:Name="PasswordBoxControl">
            </PasswordBox>
        </DockPanel>
        <DockPanel Margin="-250,0,0,-150">
            <Button Style="{DynamicResource ButtonStyle}" 
                Width="198" 
                Height="53"
                Background="#776e65"
                Content="Войти"
                Foreground="#faf8ef"
                FontSize="25"
                FontWeight="Bold" Click="EnterButton_Click">
            </Button>
        </DockPanel>
        <DockPanel Margin="210,0,0,-150">
            <Button Style="{DynamicResource ButtonStyle}" 
            Width="205" 
            Height="53"
            Background="#776e65"
            Content="Регистрация"
            Foreground="#faf8ef"
            FontSize="25"
            FontWeight="Bold" Click="RegisterButton_Click">
            </Button>
        </DockPanel>
    </Grid>
</Window>
