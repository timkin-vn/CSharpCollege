<Window x:Class="SeaBattle.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SeaBattle.Wpf.ViewModels"
        Title="Морской бой" Height="620" Width="780"
        Background="#0D1B2A"
        ResizeMode="CanMinimize">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate x:Key="CellTemplate">
            <Border Width="30" Height="30" Margin="1" CornerRadius="3"
                    Background="{Binding Background}"
                    MouseLeftButtonUp="Cell_Click">
                <TextBlock Text="{Binding Text}" Foreground="White"
                           FontSize="16" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Морской бой" FontSize="34" FontWeight="Bold"
                   Foreground="#E6F1FF"/>

        <TextBlock Grid.Row="1" Text="{Binding Status}" FontSize="15"
                   Foreground="#8FB3D6" Margin="0,6,0,12"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Ваш флот" FontSize="15" Foreground="#8FB3D6" Margin="0,0,0,8"/>
                <Border Background="#152A40" CornerRadius="8" Padding="6">
                    <ItemsControl ItemsSource="{Binding PlayerCells}"
                                  ItemTemplate="{StaticResource CellTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="10" Columns="10"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Поле противника" FontSize="15" Foreground="#8FB3D6" Margin="0,0,0,8"/>
                <Border Background="#152A40" CornerRadius="8" Padding="6">
                    <ItemsControl ItemsSource="{Binding EnemyCells}"
                                  ItemTemplate="{StaticResource CellTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="10" Columns="10"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Border>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Перетасовать" Command="{Binding ShuffleCommand}" Padding="14,6" Margin="0,0,12,0"/>
            <Button Content="В бой!" Command="{Binding StartCommand}" Padding="14,6" Margin="0,0,12,0"/>
            <Button Content="Новая игра" Command="{Binding NewGameCommand}" Padding="14,6"/>
        </StackPanel>
    </Grid>
</Window>
