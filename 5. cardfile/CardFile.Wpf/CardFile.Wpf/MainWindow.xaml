<Window x:Class="CardFile.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CardFile.Wpf.Views"
        xmlns:vm="clr-namespace:CardFile.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="Продуктовый магазин" 
        Height="650" Width="1100"
        Loaded="Window_Loaded"
        Initialized="Window_Initialized">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть" Click="MenuFileOpen_Click"/>
                <MenuItem Header="Сохранить" Click="MenuFileSave_Click"/>
                <MenuItem Header="Сохранить как..." Click="MenuFileSaveAs_Click"/>
                <Separator/>
                <MenuItem Header="Выход" Click="MenuFileExit_Click"/>
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DataGrid с ВСЕМИ 8 столбцами -->
            <DataGrid Grid.Row="0" Margin="5"
                     ItemsSource="{Binding Cards}"
                     SelectedItem="{Binding SelectedCard}"
                     AutoGenerateColumns="False"
                     SelectionMode="Single"
                     SelectionChanged="DataGrid_SelectionChanged"
                     CanUserAddRows="False"
                     IsReadOnly="True">
                <DataGrid.Columns>
                    <!-- 8 столбцов как требовалось -->
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Название" Binding="{Binding ProductName}" Width="150"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="Производитель" Binding="{Binding Manufacturer}" Width="150"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=C2}" Width="80"/>
                    <DataGridTextColumn Header="Кол-во" Binding="{Binding QuantityInStock}" Width="60"/>
                    <DataGridTextColumn Header="Произведено" 
                                      Binding="{Binding ProductionDate, StringFormat=dd.MM.yyyy}" 
                                      Width="100"/>
                    <DataGridTextColumn Header="Годен до" 
                                      Binding="{Binding ExpirationDate, StringFormat=dd.MM.yyyy}" 
                                      Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button Content="Добавить" Width="100" Margin="5" Padding="10" Click="ButtonCreate_Click"/>
                <Button Content="Изменить" Width="100" Margin="5" Padding="10" 
                        IsEnabled="{Binding IsEditEnabled}"
                        Click="ButtonEdit_Click"/>
                <Button Content="Удалить" Width="100" Margin="5" Padding="10" 
                        IsEnabled="{Binding IsDeleteEnabled}"
                        Click="ButtonDelete_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>