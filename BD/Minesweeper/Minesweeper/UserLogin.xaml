<Window x:Class="Minesweeper.UserLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008 "
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper"
        mc:Ignorable="d"
        Title="Вход в игру" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Background="#FFF0F8FF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#FF3498DB" 
                BorderThickness="1" 
                CornerRadius="0"
                Margin="20,0,20,10"
                Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Имя пользователя:" 
                           VerticalAlignment="Center" 
                           FontSize="14"
                           Foreground="#FF2C3E50"
                           Margin="0,0,10,0"
                           Width="120"/>
                <TextBox x:Name="UsernameTextBox" 
                         Width="180" 
                         Height="30"
                         FontSize="14"
                         VerticalContentAlignment="Center"
                         BorderBrush="#FFBDC3C7"
                         BorderThickness="1"
                         Padding="5"/>
            </StackPanel>
        </Border>
        <CheckBox x:Name="ContinueCheckBox" 
                  Grid.Row="2"
                  Content="Продолжить последнюю игру"
                  FontSize="14"
                  Foreground="#FF2C3E50"
                  IsChecked="True"
                  Margin="20,0,20,20"
                  VerticalAlignment="Center"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom" 
                    Margin="0,0,0,30">
            <Button x:Name="StartButton" 
                    Content="Начать игру" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,15,0"
                    Click="StartButton_Click"
                    Background="#FF3498DB"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="ExitButton" 
                    Content="Выход" 
                    Width="120" 
                    Height="35"
                    Click="ExitButton_Click"
                    Background="#FFE74C3C"
                    Foreground="White"
                    FontSize="14"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

    </Grid>
</Window>