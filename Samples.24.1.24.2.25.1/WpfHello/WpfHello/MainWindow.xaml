<Window x:Class="WpfHello.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfHello" xmlns:viewmodels="clr-namespace:WpfHello.ViewModels"
        mc:Ignorable="d"
        Title="Приветствие" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel />
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding XInput}" VerticalAlignment="Center" Margin="5" />

        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="+" Padding="5" Margin="5" Click="AddButton_Click" />
            <Button Content="-" Padding="5" Margin="5" Click="SubtractButton_Click" />
            <Button Content="*" Padding="5" Margin="5" Click="MultiplyButton_Click" />
            <Button Content="/" Padding="5" Margin="5" Click="DivideButton_Click" />
        </StackPanel>

        <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding YInput}" VerticalAlignment="Center" Margin="5" />

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Результат =" Padding="5" Margin="5" />
                <TextBlock Text="{Binding Result}" Padding="5" Margin="5" />
            </StackPanel>
            
            <Button Content="Привет!" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="5" Margin="5" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Window>
